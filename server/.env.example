# Server Configuration
PORT=8080
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/mydrive

# JWT Configuration (IMPORTANT: Change JWT_SECRET in production!)
# JWT_SECRET must be at least 32 characters for security
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_EXPIRATION=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5000

# File Upload Configuration
CHUNK_SIZE=1048576
MAX_CHUNK_SIZE=10485760
UPLOAD_TIMEOUT=600000
SESSION_LOOKUP_TIMEOUT=5000

# Upload Directory
UPLOAD_DIR=./uploads

# Client URL (for email links and redirects)
CLIENT_URL=http://localhost:5000

# ===== EMAIL CONFIGURATION =====
# Email service is optional but recommended for user notifications
# If not configured, the app will work but email features will be disabled

# Email Service Provider
# Options: 'gmail', 'sendgrid', 'custom'
EMAIL_SERVICE=gmail

# === GMAIL CONFIGURATION ===
# For Gmail, you need to use an App Password (not your regular password)
# Steps to get Gmail App Password:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate a new app password for "Mail"
# 4. Use that 16-character password here

EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Optional: Custom sender name and email
EMAIL_FROM_NAME=MyDrive
EMAIL_FROM=noreply@mydrive.com